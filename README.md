# AI Inference Reference Architecture — Assets Repository (Preview Layout)

This repository models the open assets for the AI Inference Reference Architecture (RA). It mirrors the training RA structure but emphasizes low latency, multi‑tenancy, and gateway ingress/egress.

What’s here
- Composition‑by‑size folders (OPG‑32/64/128/256) with profile variants.
- For each variant: connectivity maps (CSV), BOMs (YAML), Hedgehog CRDs (wiring.yaml, vpc.yaml), and diagrams.
- Inference‑focused READMEs describing profile choice (Minimal vs Distributed), gateway placement, and QoS implications.

Profiles
- Minimal Inference: No back‑end fabric; Front‑end+Storage converged; In‑band + OoB. Gateway is core. RDMA class disabled by default; Control/FE prioritized.
- Distributed Inference: Adds a back‑end scale‑out network for cross‑host coordination. RDMA enabled selectively for back‑end; front‑end stays multi‑tenant.

Navigation
- Compositions (by size)
  - OPG‑32 — see `compositions/OPG-32/README.md`
  - OPG‑64 — see `compositions/OPG-64/README.md`
  - OPG‑128 — see `compositions/OPG-128/README.md`
  - OPG‑256 — see `compositions/OPG-256/README.md`

Status
- This is a scaffold. Assets are placeholders until generated by the HNP agent. Tokens mirror training where a back‑end exists; Minimal variants focus on FE+Storage and gateway assets.

References
- Inference RA abstract/outline: `docs/suggestions/active/ai-inference-ref-arch/`
- XOC‑N & OPG‑M style references: see the training RA README.

